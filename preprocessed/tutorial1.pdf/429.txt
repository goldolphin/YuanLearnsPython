【PG】附录 C　自测题答案　　 403

【PG】执行到ﬂoat()时，已经太晚了 —答案已经是 0了！第二个公式也一样。

【PG】3. 可以“骗 过”int()，让它四舍五入而不是向下取整， 只需将传入 int()的数加 0.5。

【PG】右面是一个例子（交互模式中） ：
如果原先的数小于 13.5，int()会得到一个小于 14的数，这会向下取整为 13。
如果原来的数大于或者等于 13.5 ，int()会得到一个等于或者大于 14的数，
这就会向下取整为 14。 

【PG】动手试一试
1. 可以使用 ﬂoat()将字符串转换为小数：

【PG】但怎样知道这是一个数字而不是一个字符串？通过检查它的类型：
2. 可以使用 int()将十进制数转换为整数：

【PG】答案被向下取整了。
3. 可以使用 int()将字符串转换为整数：

【PG】第5章测试题
1. 对于这行代码：如果用户键入 12，answer会包含一个字符串。这是因为 raw_input()总是会得到一个字符串。

【PG】2. 可以使用 int()将小数转换为整数，结果会向下取整。

【PG】3. 可以使用 int()将字符串转换为整数。