【PG】18.5　另一个游戏——PyPong 　　 239

【PG】还需要一种方法来显示这个分数。Pygame 使用一个名为 font的模块显示文本。可以这样来使用。

【PG】建立一个 font对象，告诉 Pygame你想要的字体样式和大小。

【PG】渲染文本， 向 字体对象传入一个字符串， 它会返回一个绘制有这个文本的新 的表面。

【PG】把这个表面块移到显示表面。

【PG】术语箱

【PG】计算机图形学中，渲染（render）是指绘制某个东西，或者让它可见。

【PG】在这里， 字符串就是玩家的分数（ 不过首先必须把它从一个 int转换为一个string） 。

【PG】我们需要类似下面的代码 ，要 放在代码清单 18-4中的事件循环前面（ 而且要在paddle=MyPaddleClass([270,400]) 代码行后面） ：
score_font = pygame.font.Font( None, 50)
score_surf = score_font.render (str(score), 1, (0, 0, 0))
score_pos = [10, 10]创建字体对象

【PG】设置文本位置渲染文本到表面块Score-surf

【PG】第一行中的第一个参数（ 这里是None）可以告诉 Pygame我们希望使用什么字体（类型样式） 。通过传入 None，就是在告诉 Pygame要使用一个默认字体。

【PG】然后，在事件循环内部，我们需要这样的代码：

【PG】screen.blit(score_surf, score_pos) 把含有分数文本的表面块移到这个位置

【PG】这样每次循环时都会重绘分数文本。

【PG】我试过了，不过得到一个错误NameError！当然了，卡特，我们还没有创建points变量（我正打算这么做呢）。在创建font对象的代码前面增加这样一行代码：score = 0