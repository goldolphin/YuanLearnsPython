【PG】12.13　列表排序　　 127

【PG】这里我们的循环变量是 letter。 （之前我们使用了 looper或i、j和k之类的循环变量。）循环迭代处理（循环处理）列表中的所有值，每次迭代时，当前元素会存储在循环变量 letter 中，然后显示出来。

【PG】12.13　列表排序

【PG】列表是一种有顺序（ordered）的集合。这说明列表中的元素有某种顺序，每个元素都有一个位置，也就是它的索引。一旦以某种顺序将元素放在列表中，它们就会保持这种顺序，除非用 insert()、append()、remove()或pop() 改变列表。不过这个顺序可能不是你真正想要的顺序。你可能希望列表在使用前已经排序。

【PG】要对列表排序，可以使用 sort() 方法。

>>> letters = ['d', 'a', 'e', 'c', 'b']
>>> print letters['d', 'a', 'e', 'c', 'b']
>>> letters.sort()
>>> print letters['a', 'b', 'c', 'd', 'e']

【PG】 sort() 会自动按字母顺序对字符串从小到大排序，如果是数字，就会按数字顺序从小到大排序。有一点很重要，你要知道 sort() 会在原地修改列表。这说明它会改变你提供的原始列表，而不是创建一个新的有序列表。所以，你不能这样做：

>>> print letters.sort()

【PG】如果这样做，会得到 “None”。必须分两步来完成，就像这样：

>>> letters.sort()
>>> print letters

【PG】按逆序排序

【PG】让一个列表按逆序排序有两种方法。一种方法是先按正常方式对列表排序，然后对这个有序列表完成逆置（reverse），如下：print letter 
a 
bcde 

【PG】图灵社区会员 FreeLink 专享 尊重版权