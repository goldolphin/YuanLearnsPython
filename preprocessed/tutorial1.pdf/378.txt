【PG】第 24章　计算机仿真

【PG】24.4　时间对象

Python的日期和时间对象类在单独的 datetime 模块中定义 。datetime 模块包
含处理日期 、时间以及日期或时间之差 （delta）的类。

【PG】我们要使用的第一种对象是 datetime 对象。 （没错，这个类与模块同名 。 ）
datetime 对象包含年 、月、日、小时、分和秒。可以像这样创建一个 datetime 对
象（在交互模式中 ） ：

【PG】Python的 date 和 time 对象类
Python的 date 和 time 对象类是在 datetime 模块中定义的。date 类只包括年、月和日，而 time 类只包括小时、分钟和秒。

【PG】拆分正文部分

我们已经创建了一个名为when的datetime对象，其中包含日期和时间值。创建datetime对象时，参数的顺序（括号中的数）应当是年、月、日、小时、分钟和秒。不过如果你记不住这个顺序，也可以按任意顺序放置参数，只是要告诉Python各个参数分别表示什么，如下：

还可以对datetime对象做一些其他处理，你可以得到单个部分，比如年、日或者分。还可以得到日期和时间的一个格式化字符串。在交互模式中试试下面的代码：

Python的datetime模块包含了一些处理时间和日期的很有用的类。比如，datetime对象包含日期和时间信息。如果你只关心日期，可以使用date类，其中只有年、月和日。如果你只关心时间，可以使用time类，其中只包括小时、分和秒。术语delta的含义是“差”。这是一个希腊字母，看起来像是一个三角形（Δ）。科学和数学领域经常使用希腊字母作为某些量的简写。delta用于表示两个值之差。

除了datetime对象之外，date和time对象也非常有用。可以使用datetime模块中的datetime.date()函数来创建date对象。下面展示了如何创建一个date对象、打印date对象及访问其属性：

我们已经创建了一个名为when的datetime对象，其中包含日期和时间值。创建datetime对象时，参数的顺序（括号中的数）应当是年、月、日、小时、分钟和秒。不过如果你记不住这个顺序，也可以按任意顺序放置参数，只是要告诉Python各个参数分别表示什么，如下：

除了datetime对象之外，date和time对象也非常有用。可以使用datetime模块中的datetime.time()函数来创建time对象。下面展示了如何创建一个time对象、打印time对象及访问其属性：

有一种方法可以将datetime对象分解为其组成部分（date和time对象）：使用datetime对象的date()和time()方法。

在本章中，我们主要讨论了Python的datetime和time对象。这些对象非常有用，可用于跟踪时间和日期以及在程序中存储这些信息。在下一章中，我们将介绍如何使用Python中的模块和包。