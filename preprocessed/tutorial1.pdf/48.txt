【PG】第 2章　记住内存和变量

【PG】2.4　数字和字符串

【PG】目前为止， 我们已经为字母（ 文本）和数字创建了变量。 不过，在前面的加法例子中， Python怎么知道我们指的是数字 5和3，而不是字符“5”和“3”呢？就像前面这句话一样，正是引号带来了差别。

【PG】字符或字符序列 （字母、数字或标点符号）称为一个字符串（string）。 要告诉Python你在创建一个字符串， 就要在字符两边加上引号。 至于使用单引号还是双引号，Python并不太挑剔。单引号和双引号都是可以的：

【PG】但你必须在字符串的开头和结尾使用同样的引号。如果你在没有引号的情况下输入一个数字，Python就知道你表示的是数值，而不是字符。试试这个：

【PG】没有引号时，5和3被视为数字，因此我们得到了和。有引号时，'5'和 '3'被视为字符串，所以我们得到了“相加”的两个字符，或'53'。

【PG】你还可以将由字母构成的字符串“相加”，就像我们在第1章中看到的那样。请注意，在像这样将两个字符串相加时，它们之间没有空格，而是被紧密拼接在一起。

【PG】但在谈到字符串时，“相加”不是一个准确的说法（就像我们刚才说的那样）。当你将字符或字符串放在一起以形成更长的字符串时，有一个特殊的称呼。它不是“相加”（这仅适用于数字），而是称为拼接（concatenation）。我们说要拼接两个字符串。