【PG】24.4 时间对象

【PG】类似于datetime对象，如果指定了各个参数分别表示什么，完全可以按不同的顺序传入参数：

【PG】When creating a datetime object, the order of the parameters (the numbers in brackets) is year, month, day, hour, minute, second. But if you can’t remember that, you can put them in any order you want, as long as you tell Python which one is which, like this:

【PG】有一些其他的操作可以对datetime对象进行，比如获取每个部分的年、日、分，或获取日期和时间的格式化字符串。在交互模式下尝试一下：

【PG】A datetime object has both the date and the time. If you only care about the date, there’s also a date class that only has the year, month, and day. If you only care about the time, there’s a time class that only has the hour, minute, and second. Here’s what they look like:

【PG】就像datetime对象一样，如果指定了每个参数的含义，也可以以不同的顺序传递它们：

【PG】There’s also a way to break up a datetime object into a date object and a time object:

【PG】除了比较datetime对象的值，我们还需要了解如何比较两个datetime对象之间的差异（两个时间之间间隔多长）。

【PG】在模拟中，我们经常需要知道经过了多长时间。可以通过timedelta对象得到两个日期或时间之间的差。下面是创建两个时间差的方法：

【PG】Up to now, we have just been looking at time measured in whole seconds. But the time objects (date, time, datetime, and timedelta) are more precise than that. They can measure down to the microsecond, which is one millionth of a second.

【PG】现在，我们已经了解了datetime对象及其属性，接下来看一下如何比较两个datetime对象，以及如何找到它们之间的差异（两个时间之间间隔多长）。

【PG】在模拟中我们通常需要知道时间的差异，比如计算相对时间。timedelta类可以帮助我们得出两个日期或时间之间的差异。timedelta对象是两个时间之差。创建timedelta对象的方法是把两个时间对象相减。